spring.application.name=MessageBoard

# PostgreSQL DB config (updated for deployment)
spring.datasource.url=jdbc:postgresql://${DB_HOST:dpg-d23gmvh5pdvs739ucqfg-a.render.com}:5432/messageboard-db
spring.datasource.username=${DB_USER:renderuser}
spring.datasource.password=${DB_PASSWORD:qwerty12345}

# Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
